# Installed into /app/requirements.txt in the image (base ships torch+PIL only).
transformers>=4.44
accelerate>=0.33
pillow>=10
opencv-python-headless>=4.8  # line segmentation (no downloads, pure CV)
qwen-vl-utils>=0.0.8
sentencepiece>=0.2  # TrOCR processor tokenizer
hf_transfer>=0.1.8  # fast resumable HF downloads (grading container fetches weights at runtime)
